# Native-repo packages for Debian/Ubuntu-family Linux (PikaOS, etc.), installed
# via `apt install` by .chezmoiscripts/run_onchange_before_10-install-packages.sh.tmpl.
# One package per line, '#' comments allowed. Editing this file is what makes
# chezmoi re-run that script on the next `chezmoi apply`.
#
# This is a starting core set, not a full mirror of Brewfile — package names
# and availability vary by distro/release, so verify with `apt-cache search`
# on the actual box before assuming something here is right. Tools without a
# reliable native package (pyenv, nvm, topgrade) are installed separately by
# .chezmoiscripts/run_once_after_20-install-version-managers.sh.tmpl via their
# own upstream installers; oh-my-tmux is cloned via .chezmoiexternal.toml.

build-essential
cmake
git
curl
wget
zsh
tmux
neovim
kitty
lf

# CLI utilities
htop
btop
tree
jq
ripgrep
fd-find
fzf
silversearcher-ag
bat
git-delta
imagemagick
mosh
nmap
syncthing
wireguard-tools
ansible

# Languages / toolchains (base interpreters only — pyenv manages versions)
python3
python3-pip
pipx

# Apps from the third-party apt repo the install script configures
signal-desktop
