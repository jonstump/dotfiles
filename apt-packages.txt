# Native-repo packages for Debian/Ubuntu-family Linux (PikaOS, etc.), installed
# by install.sh via `apt install`. One package per line, '#' comments allowed.
#
# This is a starting core set, not a full mirror of Brewfile — package names
# and availability vary by distro/release, so verify with `apt-cache search`
# on the actual box before assuming something here is right. Tools without a
# reliable native package (pyenv, rbenv, nvm, topgrade, oh-my-tmux, etc.) are
# installed separately by install.sh via their own upstream installers.

build-essential
cmake
git
curl
wget
zsh
tmux
neovim
ranger

# CLI utilities
htop
btop
tree
jq
ripgrep
fd-find
fzf
silversearcher-ag
bat
git-delta
imagemagick
mosh
nmap
syncthing
wireguard-tools
ansible

# Languages / toolchains (base interpreters only — pyenv/rbenv manage versions)
python3
python3-pip
pipx

# Apps installed via the repos install.sh configures
code
signal-desktop
spotify-client
